import React from 'react';

import Localization from './Localization';

export default {
  title: 'Jmix',
  component: Localization,
};

const Template = (args) => <Localization {...args} />;

export const LocalizationConfig = Template.bind({});
